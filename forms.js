const WA='263784879938',EMAIL='info@turoyindustrial.co.zw';const params=new URLSearchParams(location.search);if(document.querySelector('#productField')&&params.get('product'))document.querySelector('#productField').value=params.get('product');function formMessage(form){const data=new FormData(form),title=form.id==='supplierForm'?'TUROY SUPPLIER APPLICATION':'TUROY QUOTATION REQUEST';let lines=[title,''];for(const [k,v] of data.entries())if(String(v).trim())lines.push(`${k}: ${v}`);lines.push('','Submitted from the Turoy website.');return lines.join('\n')}document.querySelectorAll('[data-send]').forEach(btn=>btn.addEventListener('click',()=>{const form=btn.closest('form');if(!form.reportValidity())return;const message=formMessage(form);if(btn.dataset.send==='whatsapp')window.open(`https://wa.me/${WA}?text=${encodeURIComponent(message)}`,'_blank');else{const subject=form.id==='supplierForm'?'Turoy Supplier Application':'Turoy Quotation Request';location.href=`mailto:${EMAIL}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(message)}`}}));